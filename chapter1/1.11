(define (f n)
	(cond ((< n 3) n)
	   (else
	      (+ 
	            (f (- n 1)) 
	      	    (* (f (- n 2)) 2) 
	      	    (* (f (- n 3)) 3)))))

(define (f-iter n)
	(define (f-iter-inner a b c)
		()))

(display (f 5))
(newline)
 